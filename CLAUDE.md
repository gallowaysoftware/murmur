# CLAUDE.md

The canonical agent-instructions file for this repository is
[`AGENTS.md`](AGENTS.md) (vendor-neutral, follows the
[agents.md](https://agents.md) convention).

Claude Code reads both files by convention. This pointer is kept so
that lookups for `CLAUDE.md` don't 404; treat `AGENTS.md` as the
source of truth.
